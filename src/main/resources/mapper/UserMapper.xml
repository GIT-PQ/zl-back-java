<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pq.zlbackjava.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.pq.zlbackjava.entity.User">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="question" property="question" jdbcType="VARCHAR"/>
        <result column="answer" property="answer" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, username, password, question, answer
    </sql>

    <!-- 根据用户名查询用户 -->
    <select id="findByUsername" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user
        WHERE username = #{username}
    </select>

    <!-- 根据用户名和密码查询用户 -->
    <select id="findByUsernameAndPassword" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user
        WHERE username = #{username} AND password = #{password}
    </select>

    <!-- 插入用户 -->
    <insert id="insert" parameterType="com.pq.zlbackjava.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, question, answer)
        VALUES (#{username}, #{password}, #{question}, #{answer})
    </insert>

    <!-- 更新用户密码 -->
    <update id="updatePassword">
        UPDATE user
        SET password = #{password}
        WHERE username = #{username}
    </update>

    <!-- 根据用户名查询安全问题 -->
    <select id="findSecurityQuestionByUsername" resultMap="BaseResultMap">
        SELECT id, username, question, answer
        FROM user
        WHERE username = #{username}
    </select>

    <!-- 根据ID查询用户 -->
    <select id="findById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user
        WHERE id = #{id}
    </select>

</mapper>

